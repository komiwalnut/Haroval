name: Supabase Health Ping

on:
  schedule:
    # Run 3 times per day (every 8 hours) to ensure consistent activity
    - cron: "0 0 * * *"   # Midnight UTC
    - cron: "0 8 * * *"   # 8 AM UTC
    - cron: "0 16 * * *"  # 4 PM UTC
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Verify Supabase Activity
        env:
          SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        run: |
          if [ -z "$SUPABASE_URL" ] || [ -z "$SUPABASE_SERVICE_ROLE_KEY" ]; then
            echo "❌ Missing required secrets. Please set NEXT_PUBLIC_SUPABASE_URL and SUPABASE_SERVICE_ROLE_KEY."
            exit 1
          fi

          echo "🔄 Starting Supabase health check at $(date -u +"%Y-%m-%dT%H:%M:%SZ")..."
          
          # Test 1: Query users table (count check)
          echo "📊 Testing users table..."
          USERS_RESPONSE=$(curl --fail --silent --show-error \
            "$SUPABASE_URL/rest/v1/users?select=id&limit=1" \
            -H "apikey: $SUPABASE_SERVICE_ROLE_KEY" \
            -H "Authorization: Bearer $SUPABASE_SERVICE_ROLE_KEY" \
            -H "Content-Type: application/json" \
            -H "Prefer: count=exact" \
            -w "\n%{http_code}")
          
          USERS_HTTP_CODE=$(echo "$USERS_RESPONSE" | tail -n1)
          if [ "$USERS_HTTP_CODE" != "200" ]; then
            echo "❌ Users table query failed with HTTP $USERS_HTTP_CODE"
            exit 1
          fi
          echo "✅ Users table accessible"
          
          # Test 2: Query decks table
          echo "📊 Testing decks table..."
          DECKS_RESPONSE=$(curl --fail --silent --show-error \
            "$SUPABASE_URL/rest/v1/decks?select=id,title&limit=1" \
            -H "apikey: $SUPABASE_SERVICE_ROLE_KEY" \
            -H "Authorization: Bearer $SUPABASE_SERVICE_ROLE_KEY" \
            -H "Content-Type: application/json" \
            -H "Prefer: count=exact" \
            -w "\n%{http_code}")
          
          DECKS_HTTP_CODE=$(echo "$DECKS_RESPONSE" | tail -n1)
          if [ "$DECKS_HTTP_CODE" != "200" ]; then
            echo "❌ Decks table query failed with HTTP $DECKS_HTTP_CODE"
            exit 1
          fi
          echo "✅ Decks table accessible"
          
          # Test 3: Query flashcards table
          echo "📊 Testing flashcards table..."
          FLASHCARDS_RESPONSE=$(curl --fail --silent --show-error \
            "$SUPABASE_URL/rest/v1/flashcards?select=id&limit=1" \
            -H "apikey: $SUPABASE_SERVICE_ROLE_KEY" \
            -H "Authorization: Bearer $SUPABASE_SERVICE_ROLE_KEY" \
            -H "Content-Type: application/json" \
            -H "Prefer: count=exact" \
            -w "\n%{http_code}")
          
          FLASHCARDS_HTTP_CODE=$(echo "$FLASHCARDS_RESPONSE" | tail -n1)
          if [ "$FLASHCARDS_HTTP_CODE" != "200" ]; then
            echo "❌ Flashcards table query failed with HTTP $FLASHCARDS_HTTP_CODE"
            exit 1
          fi
          echo "✅ Flashcards table accessible"
          
          echo ""
          echo "✅ All health checks passed!"
          echo "🎉 Supabase is active and responsive at $(date -u +"%Y-%m-%dT%H:%M:%SZ")"
          echo "📈 Database activity verified across users, decks, and flashcards tables"

